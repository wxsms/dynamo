# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

apiVersion: batch/v1
kind: Job
metadata:
  name: model-download
spec:
  backoffLimit: 3
  completions: 1
  parallelism: 1
  template:
    metadata:
      labels:
        app: model-download
    spec:
      restartPolicy: Never
      containers:
        - name: model-download
          image: python:3.10-slim
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            seccompProfile:
              type: RuntimeDefault
          command: ["sh", "-c"]
          envFrom:
            - secretRef:
                name: hf-token-secret
          env:
            - name: MODEL_NAME
              value: nota-ai/Solar-Open2-250B-Nota-NVFP4
            # The deployments load this snapshot directory explicitly, so the
            # download must fetch the same revision. Without it the job follows the
            # repository's current default and the workers fail on a directory that
            # was never downloaded.
            - name: MODEL_REVISION
              value: 3cc673335f4c2f41181e05a98fc2b8324d586a64
            - name: HF_HOME
              value: /shared-model-cache
            - name: HF_XET_HIGH_PERFORMANCE
              value: "1"
          args:
            - |
              set -eux
              pip install --no-cache-dir huggingface_hub==1.16.4
              hf download $MODEL_NAME --revision $MODEL_REVISION
          resources:
            requests:
              cpu: "8"
              memory: "128Gi"
          volumeMounts:
            - name: shared-model-cache
              mountPath: /shared-model-cache
      volumes:
        - name: shared-model-cache
          persistentVolumeClaim:
            claimName: shared-model-cache
