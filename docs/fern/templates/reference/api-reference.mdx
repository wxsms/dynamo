---
# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
title: API Name Reference
sidebar-title: API Name
subtitle: Complete reference for the public API surface.
---

{/*
Use this template for a manually authored, non-Kubernetes code API such as a module, package, crate,
class, function group, or protocol surface. The placeholders use Python syntax; replace the code-fence
language and syntax for the API being documented. Remove these comments and every unused section.

Do not copy this template for generated Python, Rust, or Kubernetes API pages. Update the source or
renderer that owns those pages. For an HTTP API backed by an API definition, use Fern's
EndpointSchemaSnippet, EndpointRequestSnippet, and EndpointResponseSnippet components instead of
maintaining request and response schemas by hand.
*/}

{/* State what the API controls, who should use it, and the supported version or stability level. */}

## Access the API

{/* Replace this example with the language-appropriate import, use path, package, or client setup. */}

```python
from package.module import ApiName
```

## API Surface

{/*
Repeat one Accordion for each public class, function, trait, method group, or other callable symbol.
Use a stable, page-unique id so inbound deep links do not change when surrounding content moves.
*/}

<Accordion id="api-package-module-api-name" title="ApiName (class)">
  {/* Begin with one sentence describing the symbol's purpose. */}

```python
ApiName(required_parameter: str, optional_parameter: int = 1)
```

**Parameters**

<ParamField path="required_parameter" type="str" required={true}>
  {/* Describe accepted values, validation, units, and interactions with other parameters. */}
</ParamField>

<ParamField path="optional_parameter" type="int" default="1">
  {/* Describe the default and when readers should override it. */}
</ParamField>

{/* Use Indent for nested parameter or object-field hierarchies. */}

<ParamField path="options" type="Options">
  {/* Describe the parent object. */}
</ParamField>

<Indent>
  <ParamField path="options.enabled" type="bool" default="false">
    {/* Describe the nested field. */}
  </ParamField>
</Indent>

**Returns**

{/* State the return type and describe the returned value, ownership, and important state. */}

Returns `ReturnType`.

**Raises**

{/* List each error as `ErrorType` — the condition that raises or returns it. */}

- `ErrorType` — Describe the error condition.

{/* Keep Attributes and Public methods only for classes, protocols, or traits. */}

**Attributes**

<ParamField path="attribute_name" type="str">
  {/* Describe the public attribute and whether callers may modify it. */}
</ParamField>

**Public methods**

<h4 id="api-package-module-api-name-method-name">method_name</h4>

```python
method_name(parameter: str) -> ReturnType
```

<ParamField path="parameter" type="str" required={true}>
  {/* Describe the method parameter. */}
</ParamField>

{/* State the method return type and describe the result. */}

Returns `ReturnType`.

**Example**

```python
api = ApiName(required_parameter="value")
result = api.method_name("input")
```

{/*
Add a valid source link after copying this template when it helps readers verify implementation or
version details.
*/}
</Accordion>

## Related Pages

- {/* Link to a user-facing guide that uses the API. */}
- {/* Link to the corresponding architecture or conceptual explanation. */}
- {/* Link to related API or configuration references. */}
