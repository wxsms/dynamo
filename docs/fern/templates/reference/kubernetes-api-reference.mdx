---
# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
title: ResourceKind Reference
sidebar-title: ResourceKind
subtitle: Field reference for the ResourceKind custom resource.
---

{/*
Use this template for a curated Kubernetes custom-resource reference page. Keep every field aligned
with the Go API types and the generated full Kubernetes API reference. If the target page carries a
generated-file marker, update its source or generator instead of copying this template.

Follow [Writing Kubernetes API References](../guidelines/kubernetes-api-reference.md) for the generation
pipeline, two-level indentation limit, and internal/external type-link rules. Remove these comments
and every unused section.
*/}

{/* Explain what the resource represents, when readers create it, and how the operator reconciles it. */}

{/* Replace the value below with the served API version. */}

This page documents the `nvidia.com/v1beta1` API.

<Info>
  {/* State the scope of this page and link to related resource references for shared or nested fields. */}
</Info>

## Minimal Example

```yaml
apiVersion: nvidia.com/v1beta1
kind: ResourceKind
metadata:
  name: example
spec:
  requiredField: value
```

## Spec Reference

{/*
Document desired-state fields from `spec`. Do not repeat standard `apiVersion`, `kind`, or
`metadata` behavior unless Dynamo adds a resource-specific constraint.
*/}

<ParamField path="requiredField" type="string" required={true}>
  {/* Describe purpose, allowed values, validation, and interactions with other fields. */}
</ParamField>

<ParamField path="optionalField" type="integer" default="1">
  {/* Describe the default and when readers should override it. */}
</ParamField>

<ParamField path="nestedConfig" type="NestedConfig">
  {/* Describe the parent object and link to Additional Types when the shape is reused. */}
</ParamField>

<Indent>
  <ParamField path="enabled" type="boolean" default="false">
    {/* Use the local field name inside Indent; do not repeat `nestedConfig.`. */}
  </ParamField>
</Indent>

<ParamField path="podTemplate" type="core/v1.PodTemplateSpec">
  {/* Describe how Dynamo merges or interprets the external Kubernetes type. */}

  <a href="https://pkg.go.dev/k8s.io/api/core/v1#PodTemplateSpec" target="_blank">core/v1.PodTemplateSpec</a>
</ParamField>

## Status

{/* Explain when the controller updates status and whether fields are absent before reconciliation. */}

<ParamField path="status.phase" type="ResourcePhase">
  {/* Describe lifecycle values and transitions. */}
</ParamField>

<ParamField path="status.conditions" type="[]metav1.Condition">
  Standard Kubernetes conditions reported by the controller. Document resource-specific condition
  types, reasons, and transition behavior.

  <a href="https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#Condition" target="_blank">metav1.Condition</a>
</ParamField>

<ParamField path="status.observedGeneration" type="integer">
  Most recent `metadata.generation` observed by the controller.
</ParamField>

<ParamField path="status.lastTransitionTime" type="metav1.Time">
  {/* Describe when the timestamp changes. */}

  <a href="https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#Time" target="_blank">metav1.Time</a>
</ParamField>

## Additional Types

{/* Add reused, list/map element, large, or third-level nested types here. Do not expand external Kubernetes types. */}

### NestedConfig

{/* Describe where this type appears and any cross-field validation. */}

<ParamField path="enabled" type="boolean" default="false">
  {/* Describe the field. */}
</ParamField>

### ResourcePhase

{/* List enum values and define what each state means. */}

| Value | Meaning |
|---|---|
| `Pending` | Replace with the resource-specific meaning. |
| `Ready` | Replace with the resource-specific meaning. |
| `Failed` | Replace with the state meaning and recovery behavior. |

## Inspect a Resource

```bash
kubectl get resourcekind example -o yaml
```

```bash
kubectl describe resourcekind example
```

{/* Add status, event, or log commands that help readers interpret the resource state. */}

## Related Pages

- {/* Link to the workflow that creates or updates the resource. */}
- {/* Link to related custom-resource references. */}
- {/* Link to the generated full Kubernetes API reference. */}
