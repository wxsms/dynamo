---
# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
title: Kubernetes Deployment Templates
sidebar-title: Kubernetes Templates
subtitle: Ready-to-apply Kubernetes manifests for a Dynamo deployment workflow.
---

{/*
Use this template for a page under `docs/fern/pages/recipes/kubernetes-templates/` that collects
related Kubernetes manifests. Remove these comments and every unused section before publishing.

- Keep the canonical YAML under `examples/` or `recipes/` and embed it with Fern's `<Code>`
  component. Set its `src` prop instead of copying the manifest into the page.
- Group manifests by the choice readers make, such as topology, backend, or workflow.
- Name each accordion `<filename> · <purpose>` and explain when to choose it before the code block.
- Resolve every `src` path from the destination page after copying this template.
*/}

{/* Replace this paragraph with the resource, backend, or workflow covered by the page. */}

Copy-ready Kubernetes manifests for a Dynamo deployment workflow. Each manifest is embedded from
its canonical source in the repository. Open an entry, use the copy button, and update the values
called out in its description before applying it.

## Prerequisites

- {/* Link to the canonical Dynamo Kubernetes installation page. */}
- {/* List required secrets, storage, hardware, or operator configuration. */}
- {/* State any assumptions shared by every manifest on this page. */}

Set the variables used by the examples:

```bash
export NAMESPACE=dynamo-cloud
```

Apply a template with:

```bash
kubectl apply -f <template>.yaml -n ${NAMESPACE}
```

## Template Group

{/* Rename this heading for the reader-facing choice, such as Aggregated or Generate a DGD. */}

<AccordionGroup>
  <Accordion title="agg.yaml · Baseline aggregated serving">
    {/* Explain what this manifest deploys and when readers should choose it. */}

    {/*
    Replace this placeholder with the canonical manifest for the entry.

    <Code
      src="<relative-path-to-manifest>"
      title="agg.yaml"
      language="yaml"
      maxLines={0}
    />
    */}
  </Accordion>

  <Accordion title="agg_router.yaml · Aggregated serving with KV-aware routing">
    {/* Explain the behavior this variant adds and any values readers must customize. */}

    {/*
    Replace this placeholder with the canonical manifest for the entry.

    <Code
      src="<relative-path-to-manifest>"
      title="agg_router.yaml"
      language="yaml"
      maxLines={0}
    />
    */}
  </Accordion>
</AccordionGroup>

## Verify the Deployment

```bash
kubectl get pods -n ${NAMESPACE}
```

```bash
# Add a resource-specific status check, port-forward, or inference request.
```

{/* Add expected status or output when it helps readers confirm success. */}

## Source

{/*
Link to the canonical source directory with an absolute
`https://github.com/ai-dynamo/dynamo/tree/main/<path>` URL.
*/}

## Related Pages

- {/* Link to the task-oriented Kubernetes guide that uses these manifests. */}
- {/* Link to the relevant Kubernetes API reference. */}
- {/* Link to local CLI templates when an equivalent workflow exists. */}
